services:
  browser:
    build: .
    container_name: hummingbot-log-browser
    ports:
      - "8501:8501"
    volumes:
      - ${SQLITE_DB_DIR:-./data}:/data
    environment:
      - SQLITE_DB_DIR=/data
    restart: unless-stopped

networks:
  default:
    name: hummingbot-network
